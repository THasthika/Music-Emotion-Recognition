command:
- ${env}
- ${interpreter}
- ${program}
- statB_deam
- ${args}
method: bayes
metric:
  goal: minimize
  name: val/loss
name: StatB-DEAM
parameters:
  lr:
    distribution: uniform
    max: 0.1
    min: 0.0001
  max_epochs:
    value: 40
program: run_sweep.py