command:
- ${env}
- ${interpreter}
- ${program}
- catB_mer_taffc
- ${args}
method: bayes
metric:
  goal: minimize
  name: val/loss
name: CatB-MER-Taffc
parameters:
  lr:
    distribution: uniform
    max: 0.1
    min: 0.0001
  max_epochs:
    value: 40
program: run_sweep.py