import argparse

def parse(parser, cmds=[]):
    """parse command line args to get sweep attributes

    Args:
        parser: (argparse.ArgumentParser) sub parser
        cmds (list): List of commands to accept example: [("lr", float, 0.001), ...]
    """

    
    
    return parser